publishing {
    publications {
        nebulaBom(MavenPublication) {
            // TODO java platform plugin
            artifactId = project.name + '-bom'
        }
    }
}

bintray { pkg { publications += ['nebulaBom'] } }
